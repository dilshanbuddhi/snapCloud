spring.application.name=snapcloud-api

spring.datasource.url=jdbc:mysql://localhost:3306/snapcloud?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD:1234}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=true

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME:nethushiperera03@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:}
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

server.port=8080

logging.level.org.hibernate.SQL=debug

jwt.secret=2D4A614E645267556B58703273357638792F423F4428472B4B6250655368566DF423F4428472B4B6250655368566D
jwt.expiration=3600000

# SendGrid API key (use env var)
sendgrid.api-key=${SENDGRID_API_KEY:}

# Optional: mail mode used by SendMailUtil (smtp | sendgrid)
app.mail.type=${APP_MAIL_TYPE:smtp}
app.mail.from=${APP_MAIL_FROM:nethushiperera03@gmail.com}
app.mail.sendgrid-api-key=${SENDGRID_API_KEY:}
